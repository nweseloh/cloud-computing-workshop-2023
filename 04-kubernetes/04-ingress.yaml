apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nicole-ingress
  namespace: nicole
  annotations:
    kubernetes.io/ingress.class: azure/application-gateway
spec:
  rules:
  - http:
      paths:
      - path: /
        backend:
          service:
            name: nicole-service
            port:
              number: 80
        pathType: Exact